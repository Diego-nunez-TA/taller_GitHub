Comandos básicos:
ls: saber archivos que hay y donde estas
cd: mover de directorio
cd.. : volver a directorio atras

#Usuario:
comprobar el usuario: git config --global user.name
Entrar en github: git config --global user.name "Tu Nombre"
                  git config --global user.email "Tu@email.com"

#Actualizar repo:
code prueba_javier.py: crear un archivo
git add. : para subir TODOS los cambios   (no se suben carpetas vacias)
git add prueba_javier.py: subir solo un cambio  (importante estar en el directorio donde estas)
git status: ves las modificaciones que vas a subir o las que puedes subir
git reset HEAD "nombre_archivo": quitas los seleccionaodos
git reset: los quita TODOS
git commit -m "actualizacion" : para comitear, usar el entrecomillado para el mensaje
git push

#Ramas:
git branch prueba_1
git branch --list: comprobar las que hay
git checkout prueba_1: moverte de rama
git branch -d prueba_1: borrarla
git push origin prueba_1: sincronizar con el repo

